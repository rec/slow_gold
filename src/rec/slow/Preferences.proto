package rec.slow.proto;

import "rec/gui/ThumbnailDescription.proto";
import "rec/slow/LoopWindow.proto";
import "rec/slow/RecentFiles.proto";

message Preferences {
  optional slow.proto.LoopWindow loop_window = 1;
  optional gui.ThumbnailDescription thumbnail = 2;
  optional RecentFiles recent_files = 3;
};
