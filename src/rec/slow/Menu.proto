package rec.slow;

import "rec/command/Command.proto";

message MenuEntry {
  optional Command.Type type = 1;
  optional uint32 slot = 2;
  optional string name = 3;
  optional uint32 flags = 4 [default = -1];
  optional bool is_separator = 5;
  repeated string disable_on_feature = 6;
};