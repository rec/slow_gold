package rec.audio.source;

import "rec/util/file/VolumeFile.proto";

message Segment {
  optional double begin = 1;
  optional double end = 2;
  optional string name = 3;
  optional string notes = 4;
};

message SegmentList {
  repeated Segment segment = 1;
  repeated bool selected = 2;
};