package rec.util;

message LoopPoint {
  optional uint64 time = 1;
  optional bool selected = 2;
  optional string name = 3;
  optional string notes = 4;
};

message LoopPointList {
  repeated LoopPoint loop_point = 1;
  optional uint64 length = 2;
};
